export enum AccountStatus {
  OFFLINE = "OFFLINE",
  CONNECTING = "CONNECTING",
  ONLINE = "ONLINE",
  BOOSTING = "BOOSTING",
  ERROR = "ERROR",
}

export enum StealthMode {
  NORMAL = "normal",
  INVISIBLE = "invisible",
  OFFLINE = "offline",
}

