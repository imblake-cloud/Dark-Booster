# Hoist all packages to node_modules root — required for steam-user and other
# packages that use dynamic require() and expect a flat node_modules layout.
shamefully-hoist=true
# Disable age check — redundant when using --frozen-lockfile (lockfile already pins exact versions)
minimum-release-age=0
